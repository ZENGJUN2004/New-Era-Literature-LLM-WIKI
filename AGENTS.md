# AGENTS.md — 新时代文学 LLM WIKI 运行规则（Schema）

本文件是 LLM Wiki 的 **Schema 层**：定义并约束 AI 在 ingest / query / lint 三个动作中的行为规范、命名规则、页面模板与引用格式。可理解为 `Prompt + Workflow + SOP`。

## 一、数据架构（三层）

| 层 | 目录 | 说明 |
|---|---|---|
| Raw Sources（原始数据，Source of Truth） | `raw/` | 从中国作家网采集的原文，**永不修改**，是唯一事实来源 |
| Wiki（Markdown 知识网） | `wiki/` | AI 从 raw 提炼的相互引用的知识页面，由 AI 维护 |
| Schema（规则约束） | `AGENTS.md` | 本文件，约束 AI 的一切行为 |

辅助：`index.md`（目录索引）、`log.md`（变动日志）。

## 二、数据采集（raw/）

- 优先使用中国作家网检索 API（`search.people.cn/search-platform/front/searchByType`），详细用法见采集脚本 `scripts/fetch_api.py` 与站点经验。
- **所有原文保留时间戳**：文件名/元数据中必须含发布时间（年-月-日），这是本知识库的核心检索维度。
- 采集成 `.json`（含 34 字段原始元数据 + 完整正文 HTML）存 `raw/json/`，并生成可读 Markdown 存 `raw/markdown/`。
- 原始文件永不改写；如需更新以新文件追加，旧文件保留。
- **版权注意**：优先采集作协官方原创新闻/评论/访谈；期刊转载与会员投稿作品仅做元数据索引，正文二次利用需另行确认授权。

### 2.1 反爬采集纪律（强制）

1. **请求间隔**：两次请求间强制随机等待 3~8 秒（脚本默认，不得低于该值）；每分钟请求数上限 10 次。
2. **失败退避**：单页失败按指数退避重试（10s/20s/40s），最多 3 次；超限**跳过该页，禁止硬顶**。
3. **分批切片**：大批量采集必须按天/周切分多次执行，单次执行用 `--max-pages` 限制规模（页大小 20），严禁一次性灌满。
4. **串行执行**：同一时段只允许一个采集任务；不得并行多任务。
5. **随手验证**：每次采集后核对时间字段与条数，防止翻页漏采或入库错位；若连续出现封禁特征（非正常状态码/IP异常），立即停止，间隔数小时后再试。

## 三、Wiki 页面规范

### 3.1 知识分类与目录命名

`wiki/` 下按主题目录组织，最多两层。

```
wiki/
├── 人物/              # 作家、评论家、学者、编辑档案
├── 作品/              # 单篇作品/鸿篇作品分析
├── 主题/              # 文学主题、思潮、现象
├── 概念/              # 文学概念、术语、理论
├── 流派/              # 文学流派、代际
├── 机构/              # 作协、出版机构、奖项、刊物
├── 事件/              # 文学界重要事件、活动
└── 时间线/            # 按年-月-日组织的编年史
```

### 3.2 文件命名

- 一律使用实体/主题的标准名称作为文件名，例如 `人物/莫言.md`、`主题/新时代现实主义.md`。
- 同一实体只建**一个**页面；重名冲突时以更常见的通称命名，并在页内记录别名。
- 文件名不使用日期（时间维度由「时间线」目录和页面内时间戳承担）。

### 3.3 页面模板（Markdown）

每个 Wiki 页面统一包含以下区块（`---` 间的 YAML frontmatter 为结构化元数据）：

```markdown
---
type: 人物 | 作品 | 主题 | 概念 | 流派 | 机构 | 事件 | 时间线
name: 实体标准名称
aliases: [别名1, 别名2]
created: 2026-09-06
updated: 2026-09-06
sources:
  - id: raw/json/xxx.json          # 引用的原始来源
    url: http://www.chinawriter.com.cn/...
    pubdate: 2026-08-13            # 原文发布时间（精确到日）
related: [../人物/莫言.md]          # 相关页面链接
tags: [标签1, 标签2]
---

# {{name}}

<!-- 概述：2-3 句说明本实体是什么 -->

## 关键信息
- **生卒/机构/代表作**：...
- **时间线**：与本文相关的关键年月日节点

## 详细内容
（AI 提炼自 raw 来源，分小节组织，注意准确性与可追溯）

## 关联
- 相关人物/作品/主题（用相对链接、双向引用）

## 来源
- [原文标题](url)＠ 2026-08-13
```

**时间可追溯规则**（核心）：
- 页面内一切**有具体日期的事件/观点/数据**必须标注到 `YYYY-MM-DD`，不得仅写"近年""最近"。
- 保留「时间线」小节，把该实体相关的重要事件按时间正序排列，精确到年月日。
- 前后信息来源的 datum 不同或相互矛盾时，分别记录并注明各自出处日期。

### 3.4 链接与交叉引用

- 用相对路径 `[人物/莫言.md](人物/莫言.md)` 互相链接，形成知识网。
- 一个实体被多处提及时应从各处链接到其唯一页面，避免信息碎片化。
- 建立链接时如目标页尚不存在，可在 `index.md` 预留待建条目。

## 四、三个核心动作

### 4.1 Ingest（资料新增）
新 raw 文档进入后，AI 依次：读取 → 判断涉及哪些既有实体/主题 → 新建缺失页面 / 更新对应页面 → 补全「时间线」→ 更新 `index.md` → 记录 `log.md`。一个 raw 可能触发多页更新。

### 4.2 Query（信息查询）
先查 Wiki 而非原文；发现优质结果或新结论时，将结果写回为新 Wiki 页面或更新既有页面（每次 QA 都是知识沉淀）。回答中涉及具体日期时标注来源与日期。

### 4.3 Lint（纠错与整理）
定期检查：孤儿页面、死链接、过期内容、相互矛盾、引用缺失、需拆分的大页面、缺失的重要主题/人物/作品；主动建议补充内容和待读资料。

## 五、每日同步机制（Daily Sync）

中国作家网每日更新，须建立"每日采集 → 增量 ingest"的持续运转机制，保证 Wiki 与官网同步。

### 5.1 自动化流水线（scripts/daily_sync.py）

单条命令完成「采集当日（或回溯 N 日）数据 → 转 Markdown → 生成待 ingest 清单 → 记录 log.md」：

```
python scripts/daily_sync.py --key <关键词> [--date YYYY-MM-DD | --days-back N] [--searchType 0|1|2|3]
```

- `--key` 可多次指定（如 `--key 文学 --key 评论 --key 访谈`），多关键词共用同一限速器。
- 幂等：已采集过的文档（raw/json 已存在）自动跳过，重复运行不产生重复请求。
- 产出任务清单 `tasks/ingest_YYYYMMDD.md`，交由 LLM 执行 ingest。
- 推荐用 Windows 任务计划程序（schtasks）每日定时运行，如每日 09:00 采集前一天。

### 5.2 每日增量 ingest（LLM 环节）

任务清单生成后，AI 依次处理清单中每篇文档（遵循 4.1 Ingest 规则）：
1. 读取 raw 文档，判断触发哪些既有/新实体；
2. 新建缺失页面或更新对应页面，补全「时间线」（精确到日）；
3. 处理完一篇即在任务清单中勾选并更新 `index.md`；
4. 全部完成后记 `log.md`（【Ingest】条目），并删除已完成的旧任务清单或归档。

### 5.3 同步纪律

- 每日同步仍遵守 2.1 反爬纪律：默认只采最近 1 天，忌多日多词一次灌满；大范围回溯按周/月切片。
- 每日新增量少（一般 10~20 篇），应在当天完成 ingest，避免任务清单积压。
- 每周做一次 Lint（4.3），检查同步后的孤儿/死链/矛盾。

## 六、Style 与准确性

- **准确性优先**：所有事实性/日期性陈述必须有出处（指向 raw 或来源 URL）。绝不编造作家、作品、日期。不确定时标注「存疑」。
- AI 语气保持客观、中立的学术整理风格，不做评论性发挥，只做事实与脉络的梳理。
- 对期刊转载文的文学观点，注明原文作者与期刊来源，不将二次转载观点当作作协官方立场。
